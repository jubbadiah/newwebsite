{% extends "partials/base.html.twig" %}
{% block title %}Buy{% endblock %}

{% block content %}

        <div class="grid-container">
            <div class="grid-header">Track Name</div>
            <div class="grid-header">Track Length</div>
            <div class="grid-header">BPM</div>
            <div class="grid-header">Genre</div>
            <div class="grid-header">Format</div>
            <div class="grid-header">Price</div>

        </div>

{% for product in products %}

        <div class="grid-row">
            <div class="row">{{ product.trackname }}</div>
            <div class="row">{{ product.tracklength }}</div>
            <div class="row">{{ product.bpm }}</div>
            <div class="row">{{ product.genre }}</div>
            <div class="row">{{ product.format }}</div>
            <div class="row">£{{ product.price }}</div>
        </div>
{# {{ product.trackname }} #}
{% endfor %}


{% endblock %}


{# product(db).trackname(row) #}
{# To do: Create a table using css grids  #}
